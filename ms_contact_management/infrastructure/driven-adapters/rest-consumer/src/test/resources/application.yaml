adapters:
  rest-consumer:
    timeout: 1000
    resources:
      basicKit: localhost:8081/api/v1/alert-management/alert-client/basic-kit
      retrieveInfo: localhost:8081/api/v1/alert-management/alert-client/basic-kit
    client_id: "${CLIENT_ID}"
    client_secret: "${CLIENT_SECRET}"
    message_id: "${MESSAGE_ID}"
