dependencies {
    compile project(':model')
    compile 'org.springframework:spring-context'
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
    compile group: 'com.amazonaws', name: 'aws-java-sdk-sqs'
}
