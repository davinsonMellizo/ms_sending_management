adapters:
  entries:
    reactive-web:
      path-base: /api/v1/alert-management
      alert: /alert
      campaign: /campaign
      consumer: /consumer
      category: /category
      priority: /priority
      provider: /provider
      remitter: /remitter
      service: /service
      schedule: /schedule